const path = require('path');

module.exports = {
  'config': path.resolve('dist/src/config', 'config.js'), // config 파일 경로 설정
  'models-path': path.resolve('src/models'), // models 경로 설정
  'seeders-path': path.resolve('src/seeders'), // seeders 경로 설정
  'migrations-path': path.resolve('src/migrations'), // migrations 경로 설정
};