/*
//migration을 위한 설정 파일

const path = require('path');

module.exports = {
  'config': path.resolve('dist/src/config', 'config.js'), // config 파일 경로 설정
  'models-path': path.resolve('dist/src/models'), // models 경로 설정
  'seeders-path': path.resolve('dist/src/seeders'), // seeders 경로 설정
  'migrations-path': path.resolve('dist/src/migrations') // migrations 경로 설정
};

*/