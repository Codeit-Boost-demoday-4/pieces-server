/*
import { Router } from 'express';
import BadgeController from '../controllers/badge.controller';

const router = Router();

// 그룹이 보유한 배지 조회
router.get('/groups/:id/badges', BadgeController.getGroupBadges);

// 배지 조건 확인 및 부여
router.post('/groups/:id/check-badges', BadgeController.checkAndAwardBadges);

export default router;
*/